{% set model = dbt_model('dim_books') %}

cubes:
  - {{ model.as_cube() }}
    dimensions:
      {{ model.as_dimensions() }}
      - name: book_key
        sql: "{CUBE}.book_key"
        type: string
        primary_key: true
        public: true
